# Lab 6 - Step Functions Configuration
# ⚠️ IMPORTANT: Update the username with YOUR assigned username

# REQUIRED: Your assigned username (user1, user2, user3, etc.)
username = "user1"

# Optional: AWS region (default: us-east-1)
aws_region = "us-east-1"

# Optional: Log level for Lambda functions (default: INFO)
log_level = "INFO"

# Optional: CloudWatch log retention in days (default: 7)
log_retention_days = 7

# Optional: API Gateway stage name (default: prod)
api_stage_name = "prod"
